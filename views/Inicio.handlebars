<main>
  <h1>Inicio</h1>
  {{nombre}}

  <h2>usuarios:</h2>
  <div class="custom-grid">
    {{#each users}}
      <div class="card">
        <h3>{{this.nombre}}</h3>
        <h4>{{this.apellido}}</h4>
      </div>
    {{/each}}

  </div>

</main>