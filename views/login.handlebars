<main>
  <form
    class="center flex flex-col gap-2 w-1/5 shadow p-6 rounded-md bg-white dark:bg-[#222]"
    action="/login"
    method="post"
  >
    <h1 class="text-3xl mb-4">Login {{tipo}}</h1>
    <input type="text" name="tipo" value="{{tipo}}" hidden />
    <input class="input" placeholder="Correo" type="email" name="correo" />
    <input class="input" placeholder="Clave" type="password" name="clave" />
    <button class="btn-primary">Entrar</button>
    <a href="/registro?tipo={{tipo}}" class="link-2">Crear cuenta</a>
    <a href="#" class="link-2">Olvide mi contraseña</a>
  </form>
</main>